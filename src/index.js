import React from 'react';
import ReactDOM from 'react-dom/client';
import CreateandReadIdCardQRCode from './Features/CreateandReadIdCardQRCode';
import './index.css';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <CreateandReadIdCardQRCode />
  </React.StrictMode>
);
